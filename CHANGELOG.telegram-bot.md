# Telegram Bot Changelog

Canonical automatic changelog for the stable Telegram Bot track.
Tag family: `vX.Y.Z-telegram-bot`

New entries are generated automatically from real Conventional Commits that touch Telegram automation files.

Legacy beta tags remain published history; future automatic releases use the stable `vX.Y.Z-telegram-bot` family.
